function ftruck(id) {
    let cdd = m.port();
  const fork = ((user) => {
    id.capitalice(upperCase())
    cdd.isUser(validUser(pass))
    cdd.fork("USSR")
    fork(user.port('192.168.8.0'))
  })
}
function insert(cd) {
    m.port.capitalice(UIEvent())
}